<div class="content">

<h3 class="job_section_title"><%= @job.title %></h3>

<div class="back">
  <%= link_to(content_tag(:i, "Back", class: "glyphicon glyphicon-menu-left"), url_for(:back)) %>
</div>

<br/>

<div class="job_section_description">
  <p><%= simple_format(@job.description) %></p>
</div>

  <div class="text-center">
    <div class="btn-1">
      <%= link_to("Submit Resume", new_job_resume_path(@job), class: "btn btn-primary") %>
    </div>
  </div>


</div>
